# Environment: development | production
NODE_ENV=development

# Server Configuration
PORT=5000
HOST=0.0.0.0

# MongoDB Connection
# For local development:
MONGO_URI=mongodb://localhost:27017/fitness-app

# For production (Atlas):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/fitness-app?retryWrites=true&w=majority

# JWT Configuration
# Generate secure secrets: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_EXPIRE=1h

# Refresh token for seamless re-authentication
JWT_REFRESH_SECRET=your-refresh-token-secret-min-32-chars
JWT_REFRESH_EXPIRE=7d

# CORS Configuration
# For production, list your frontend URLs
# CORS_ORIGIN=http://localhost:8081,https://yourdomain.com
CORS_ORIGIN=
